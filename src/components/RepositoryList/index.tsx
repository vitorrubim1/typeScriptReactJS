import React, { Component } from 'react';

export default class RepositoryList extends Component { //Propriedades para que o código daq de dentro tenha acesso as interfaces
    componentDidMount() { }

    render() {

        return (
            <ul> </ul>
        );
    }
} 